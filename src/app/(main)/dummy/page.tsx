"use client";

import React from "react";

const dummy = async () => {
  // const data = await api.todo.showAllPublicTodos();
  // console.log(data);

  return <div>dummy</div>;
};

export default dummy;
